# EXPECTED_ERROR_TYPE: FragmentCycle
fragment FragmentA on User {
  id
  name
  ...FragmentB
}

fragment FragmentB on User {
  email
  ...FragmentA
}

query GetUser($id: ID!) {
  user(id: $id) {
    ...FragmentA
  }
}
