# DISABLED: This test is disabled because circular input object reference validation
# is not yet implemented in libgraphql. Once the CircularInputFieldChain validation
# is added, rename this file to .graphql to enable the test.
#
# EXPECTED_ERROR_TYPE: CircularInputFieldChain
type Query {
  test(input: InputA): String
}

input InputA {
  field: InputB
}

input InputB {
  field: InputA
}
