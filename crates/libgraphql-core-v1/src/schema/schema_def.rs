/// A fully validated, immutable GraphQL schema.
///
/// Produced by
/// [`SchemaBuilder::build()`](crate::schema::SchemaBuilder::build).
/// Task 16 will provide the full implementation.
#[derive(Clone, Debug, PartialEq)]
pub struct Schema {
    _private: (),
}
